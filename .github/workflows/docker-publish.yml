name: Docker Build & Publish

on:
  push:
    branches: [ "main" ]
    # paths:
    #   - 'internal/updater/**'
#   workflow_dispatch:

env:
  IMAGE_NAME: edustartech/edustartech:app-release-manager

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    # defaults:
    #   run:
    #     working-directory: internal/updater

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: Dockerfile
          push: true
          tags: ${{ env.IMAGE_NAME }}
          cache-from: type=registry,ref=${{ env.IMAGE_NAME }}
          cache-to: type=inline

      - name: Notify Dokploy
        if: success()
        run: |
          curl -X 'POST' \
          "https://dokploy.edustartech.com/api/deploy/nDu7FC_Df8_qN_w7lqaB6" \
          -H 'accept: application/json' \
          -H 'Authorization: Bearer nHGvJdJcvpNnZsJNUGzdemOsuZedMZtVeFoICiqvBvywlXpXWDesuTyPtlugGPER' \
          -H 'Content-Type: application/json' \
          -d '{
              "json":{
                  "applicationId": "others-app-release-manager-6uiyvx"
              }
          }'
